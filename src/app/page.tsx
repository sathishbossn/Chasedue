import CinematicHome from '@/components/landing/cinematic-home'

/**
 * Marketing landing — cinematic layout (#0F0F0F / #FF6B00).
 * Portal (`/portal/[id]`), API routes, and invoice PDF generation are unchanged.
 */
export default function HomePage() {
  return <CinematicHome />
}
