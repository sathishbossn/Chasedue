import { Text, View } from 'react-native';

export function ChaseInfo() {
  return (
    <View className="gap-1">
      <Text className="text-xs text-gray-500">
        Chase history will appear here (last WhatsApp chase, count, etc.).
      </Text>
    </View>
  );
}

